<h1>Listing categories / </h1>

<table>
  <tr>
    <th>Id</th>
    <th>Level1</th>
    <th>Level2</th>
    <th>Level3</th>
    <th>Level4</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @categories.each do |category| %>
  <tr>
    <td><%= category.id %></td>
    <td><%= category.level1 %></td>
    <td><%= category.level2 %></td>
    <td><%= category.level3 %></td>
    <td><%= category.level4 %></td>
    <td><%= link_to 'Show', category %></td>
    <td><%= link_to 'Edit', edit_category_path(category) %></td>
	<td><%= link_to 'Contests', category_contests_path(category) %></td>	
    <td><%= link_to 'Destroy', category, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Category', new_category_path %><span> | </span> 
<%= link_to 'Sports', sports_path %>