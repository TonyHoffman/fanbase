<h1>Listing teams</h1>

<table>
  <tr>
    <th>City</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @teams.each do |team| %>
  <tr>
    <td><%= team.sport_id %> | <%= team.city %> </td>
    <td><%= link_to 'Edit', edit_sport_team_path(@sport, team) %></td> 
	<td><%= link_to 'Show', sport_team_path(@sport, team) %></td>
	<td><%= link_to 'Players', team_players_path(team) %></td>
	<td><%= link_to 'Contests', team_contests_path(team) %></td>	
    <td><%= link_to 'Destroy', sport_team_path(@sport, team), 	:confirm => 'Are you sure?', 
																method: :delete  %></td>
   </tr>
<% end %>
</table>

<br />

<%= link_to 'New Team', new_sport_team_path %> | 
<%= link_to 'Sports', sports_path %>
